# 도심 고밀도 확장 — 2026-09-16

상태(2026-09-17): 배경 **24/24조각 생성·전체 합성 완료**,4608×3072px의 빈 영역0픽셀. 사용자가 승인한 `background_1_5_retry1` 재생성이73.071초/$0.087243에 성공했다. 총28회 시도,27회 성공. 확정 비용 **$2.316011**, 기존 실패1건의 미확정 예약$0.75 포함 **$3.066011**. 광화문·종로타워 배치 및 보신각 가림 해제 시안도 있다. **배경 생성 완료와 제품 완성은 다르다.** 품질 검수·최종 통합·신규 배포는 미완료이며 기존 공개본을 유지한다.

## 최신 결과 보기

결과 폴더: `prototype2/work/dense-city-20260916-source/` (로컬 전용, Git/공개 배포 제외).

- `dense_full_24_preview.png`: 전체24조각 + 광화문/종로타워 배치 후보,1536×1024. 미생성 회색 영역이 없다.
- `dense_full_24_candidate.png`: 같은 후보의4608×3072 원본. `dense_landmarks_preview.png`/`dense_landmarks_candidate.png`도 최신 전체 합성본으로 갱신했다.
- `gwanghwamun_placement_candidate.png`: 광화문 배치 확대. 생성 원출력은 원본보다 세로 비율이 길다. 비균등 변형 없이 균등 축소한 후보는 원본 대비 너비−9.73%/높이+10.39%; 정확한 형상 통과로 처리하지 않았다.
- `bosingak_dense_reveal_candidate.png`: 가림 해제55% 정지 시안. 원본 스프라이트 재사용, 마스크 밖 배경 변경0. 추정 마스크가 건물 일부를 자르는 경계가 보여 다듬어야 한다. 지도에서 실제 상호작용 검증 완료를 뜻하지 않는다.
- `geometry_checks.json`: 선택 구조점 자동 비교19/24통과. 재생성1_5는 통과했다. `0_0`, `1_0`, `3_3`, `3_4`, `3_5`는 상관 신뢰도 미달로 수동 검수 필요. 남쪽 일부 가로수 추가도 관찰됐다. 자동 점검 통과는 모든 지붕/도로의 정확성을 보장하지 않는다.
- `seam_review_index.json`: 인접 경계38개의 비교 이미지. 재생성 구역의 위·왼쪽·아래 비교를 직접 확인했고 큰 단절은 보이지 않았으나, 전체 도로4px 수치 검수는 미완료다. 광화문 북쪽 겹침의 제거 전 지붕 잔상도 확인 대상.
- `generation_tasks.json`: 실제 OpenRouter→OpenAI 프롬프트와 참조 목록. `report.json`의 `budget_accounting`은 실제 비용과 예약을 분리한다. 총비용은 실패 건 때문에 여전히 미확정이며 예약액을 실제 과금으로 간주하지 않는다.
- `full_background_completion.json`: 전체14,155,776픽셀 채움·기존 실패 이력 보존·재생성 원출력SHA·비용·미완료 관문 확인 결과. `finish_full_background.py`로 재검증할 수 있다.
- 남은 순서: 낮은 신뢰도5장/식재/경계/광화문 비율/보신각 마스크 검수·필요 보정 → 실제 도로 차량/브라우저 통합 검증 → 모든 관문 충족 시 배포. 전체32회까지4회, 보수적 예산$1.933989가 남지만 새 호출마다$0.75 예약 검사는 유지한다.
- 코드 검증: Python127개, 고밀도 Node3개 통과. 유료 가이드 호출0회. 기존 원격 저장소/배포본 변경 없음.

아래는 단계별 이력이며 최신 수량·비용은 위 요약이 기준이다.

## 진행 이력과 재개 조건

- **재생성 예산 전환 승인:** 보정1회분을 배경 재생성1회로 전환했다. 그룹 한도는 배경25·광화문1·보신각 배경1·대체2·보정3으로 총32회 유지. `retry_reallocation_approval.json`에 기록했다. 기존 실패 파일/비용 미확정 기록은 보존하고, 새 이름 `background_1_5_retry1`에 새 이웃 문맥·intent·결과를 저장했다. 실행 전 승인 검토 시간 초과1회는 실제 API 요청이 아니며, 재검토 후 유료 생성은 정확히1회 실행했다.
- **중단 조건 변경 승인:** 사용자가 미확정 `background_1_5`의$0.75 예약을 유지한 채 후속 생성을 계속하도록 승인했다. 실제 비용은 null/미확정으로 보존하며0으로 처리하지 않는다. 예외는 이 한 건에만 적용하고 새 실패는 중단한다. 총$5/32회·그룹별 한도·동일 요청 자동 재시도 금지는 유지한다. `reserved_failure_approval.json`과 `resume_reserved.py`에 기록했다. 아래 '비용 확인 전 모든 호출 중단'은 변경 전 정책 이력이다.
- 재개 점검: OpenRouter Upstream Requests를 새로고침해도 성공8건만 보였으며 실패 건의 과금은 확인되지 않았다. 추가 유료 호출은0회다. `openrouter_style.py`와 `seam_lab.py`에 HTTP 상태·검증된 generation ID·실패 소요 시간 기록을 추가했다. 예외 본문/비밀키는 저장하지 않으며 실패 시 중단 정책은 유지한다. 관련 오프라인 테스트14개 통과. 이 변경으로 과거 실패 건의 ID나 비용을 복원한 것은 아니다.
- 일괄 승인: 남은 지도와 계획된 랜드마크의 원본·스타일·기존 생성물·겹침/보정 마스크를 기존 OpenRouter→OpenAI로 전송. 총$5/32회와 그룹별 한도는 그대로다. 로컬 `full_scope_approval.json`에 기록했다.
- 배경 완료: `2_2`, `2_3`, `2_4`, `2_5`, `1_2`, `1_3`, `1_4`. 실패 `1_5`를 포함한17조각의 완성 출력이 없다. 실패 요청을 자동 재시도하지 않는다.
- 전체24조각의 구조 검증점을 요청 전에 고정했다. 완료7조각은 선택 검증점의 자동 상관/크기 시험을 통과했으나, 이것은 전체 도로 경계·모든 건물·최종 미감 통과가 아니다.
- `prototype2/work/dense-city-20260916-source/dense_progress_preview.png`: 현재7조각의1536×1024 미리보기. 회색은 미생성 영역이다. `dense_background_candidate.png`는4608×3072 부분 합성 원본이다. 좌표 변형 없이 겹침 색감 보정과32px 혼합을 적용했다.
- 자동 비교는 `geometry_checks.json`, 합성 내역은 `assembly_report.json`. 전체 수동 검수와 랜드마크3개 통합은 미완료이며 공개 승인 값은 false다.
- OpenRouter의 Generations/Upstream Requests 화면에서 성공8회의 내역은 확인했다. 실패 요청의 별도 기록/비용은 보이지 않아 무과금으로 확정하지 않았다. 확정 비용과 미확정 예약을 합친 보수적 금액은$1.433880이다.
- 재개 전 실패 요청의 과금을 제공자 기록으로 확인해야 한다. 원래 실패 이력·intent를 보존하고, 비용과 호출 한도를 재계산한 뒤 미완성 항목을 재계획한다. 동일 요청의 무조건 재실행이나 비용0 처리로 안전장치를 우회하지 않는다.
- 아래의3회/$0.247370 및 외부 전송 승인 대기 기록은 이전 단계 이력이다. 현재 중단 원인은 **과금 미확정**이다.

## 승인한 범위

- 경복궁–광화문–종각 약1.65㎢: `[126.971,37.568,126.984,37.581]`.
- 정사영 방위22.5°·내려다보기30°, 100m당256px. 기존 그림을 확대하는 작업이 아니다.
- 건물 실루엣과 경계 여백을 포함한4608×3072px, 생성 코어768px의6×4구역.
- 생성 요청1024², 코어 둘레128px 문맥, 인접 요청256px 겹침.
- 독립 객체3곳: 종로타워·보신각 원출력 재사용 우선, 광화문 새 후보. 다른5곳 독립 제작은 다음 차수.
- 이번 이미지 예산 총$5, 호출 최대32회. 승인된1회 전환 후 배경25·광화문1·보신각 재구성 배경1·조건부 기존 객체 대체2·국소 보정3.
- 과금 미확정/가격 확인 실패/예산 부족 시 중단. 자동 재시도·모델 변경·예산 증액 없음.
- 기존 AI 가이드의$1 영구 원장과 별개다. 이번 UI 시험은 가이드 유료 호출을 하지 않는다.

## 표시·통합 구현

- `city-dense-pilot` 별도 형식. 기존 공개본과 과거 장면은 변환 없이 기존 렌더러로 읽는다.
- 배경512px 표시 타일, 해상도1/0.5/0.25. 1152×768 저해상도 전체 배경을 먼저 로딩한다.
- 현재 화면을 우선하고 주변 한 줄은 남은 캐시 범위에서 읽는다. 동시 요청4, 디코딩 캐시 최대48개.
- 실패한 표시 타일을 반복 요청하지 않으며 저해상도 배경을 유지한다. 새로고침 시 다시 시도할 수 있다.
- 객체·히트·보신각 패치는 영역별 PNG와 전역 위치로 저장한다. 전체 크기 마스크를 객체마다 만들지 않는다.
- 광화문 중심50%, 최대100%, 전체 보기·미니맵·세 명소 바로가기.
- 도감 ID·36곳 관광자료·비 효과·가이드 서버는 유지한다. 실제 길찾기나 실측 복원으로 표시하지 않는다.

## 생성·배포 관문

1. 전체 원본의3D 로딩·범위·동일 투영을 확인한다. 깊이별100m=256±1px, 조각 공통점≤1px.
2. 첫 인접2장을 생성하기 전에 구조2점과 크기1개를 고정한다. 밀도·화풍·좌표·연결 검증 후 나머지를 생성한다.
3. 출력 구조 오차≤16px·너비변화±15%, 주요 도로 경계≤4px, 잘린 지붕·중복 객체·빈 영역 없음.
4. 보신각 고도·높이의 추정 상태는 별도로 유지한다. 기술 검증을 측량 정확도나 사용자 미감 승인으로 바꾸지 않는다.
5. `delivery.json`의 검수 기록이 충족되어야 `dense_snapshot.py`가 표시 타일을 내보낸다.
6. `city_release.py --snapshot <새 스냅샷>`은 매니페스트에 명시된 PNG와 정제 JSON만 복사한다.
   새 지도는 매니페스트 SHA를 `snapshotSha256`으로 사용자 지시 배포 기록에 연결한다. 권리 확인 완료는 아니다.
7. 검증용 합성 데이터에는 `testFixture=true`를 기록하고 배포를 거부한다. 실패·부분 결과로 현재 공개본을 교체하지 않는다.

## 현재 검증

- Python124개, Node17개 통과.
- 고밀도 렌더러: 데스크톱1440×1000·모바일390×844 진입, 이동, 세 명소 선택, 보신각 전환/복원, 타일 장애 후 배경 유지.
- 진단 데이터 시험에서 캐시 최대48개, 오류0, 유료 호출0.
- 기존 종로 배포용 UI의 도감·검색·날씨·차량·보신각·모의 가이드 회귀 통과.
- 위 UI 시험은 실제 AI 생성 결과의 품질 검증이 아니다.
- 진단 기록: `prototype2/work/dense-renderer-qa-20260916/`.
- 추가 진단: `prototype2/work/dense-renderer-qa-20260916-v2/`. 차량 시간 진행·명소별 화면·전체 보기 포함. 데스크톱 캐시48/모바일40개, JS 오류0. 의도적으로503을 반환한 타일은 저해상도 배경으로 대체했다.
- 기존 지도 회귀: `prototype2/work/dense-legacy-qa-20260916/`.

## 원본 확보와 생성 중단 기록

- 최신 무과금 개선: `improve_pair.py`로 기존 보정물을 지면 경계에 맞춰 합성했다. 건물 보호 영역과 보정 마스크 밖은 원본 생성물과 정확히 같다. 두 번째 조각에는RGB 중앙값(-3,-6,-3) 색감 보정과32px 겹침 혼합을 적용했으며 좌표 이동·기하 변형은 없다. 시안은`first_pair_v2.png`, 비교는`ground_v2_comparison.png`.
- 고정한 지붕 검증점의 경사도 패치 상관 비교에서 이동 최대2px, 선택 변 길이 변화 최대0.465%, 원시 겹침 도로 패치 이동 최대2.236px였다. 지붕 대응점 표시와 도로 확대를 직접 검토했다. 마지막 도로 패치 신뢰도는NCC0.5001로 자동 기준0.6미달이므로 수동 검토 사항으로 분리했다. `first_pair_gate.json`은 **선택 검증점 기준 다음 생성 시험 진행 판단**이며, 전체 지리 정확도·배포 승인은 아니다. 세부 식재·외벽은 여전히 AI 재해석이다.
- 다음 후보`background_2_4`의 원본·검증점·겹침·프롬프트를 준비했다. 그러나 실행 전 자동 검토가 새 원본/생성물의 외부 전송은 첫2장+보정1회 범위를 넘어선다고 차단했다. 사용자 '중간에 멈추지 말고 계속' 지시만으로는 해당 전송 승인을 충족하지 못한다는 사유다. 추가 POST·비용 없음, 누계3회/$0.247370 유지. `blocked_requests.json`으로 무승인 실행도 차단했다.
- 재개에 필요한 일괄 범위: 남은 지도22조각의 VWorld 원본, 기존 생성물/스타일/색상 참조, 겹침 및 보정 마스크, 계획된 광화문·보신각·종로타워 참조를 기존 OpenRouter→OpenAI에 전송하여 생성·검수·보정. 총$5/32회 및 그룹별한도, 가격/과금 불명확 시 중단, 배포 품질 관문은 그대로 유지한다. 비밀키·원본 관광 DB·그 외 로컬 파일은 이미지 입력에 포함하지 않는다.
- 아래 보정 경계 미통과 기록은 앞선 직선 다각형 후보의 이력이다. 개선 후보로도 전체24조각 검증이 끝났다는 뜻은 아니다.
- 최신 보정(2026-09-17): 사용자가 추가 보정1회를 별도로 승인했다. OpenRouter→OpenAI `plaza_repair_1`은43.103초/$0.078726, **누적3회/$0.247370**. 추가 유료 호출은 하지 않았다.
- `plaza_repair_comparison.png`는원본/이전/AI원출력 비교다. 중앙 광장 장식 조경은 줄었으나 AI가 허용 마스크 밖1,013,977픽셀도 변경했다(채널 평균 절대차13.36/255). 이는 주로 전역 색감·세부 변경이며 해당 픽셀 수가 모두 구조 이동을 의미하지는 않는다.
- 허용 영역만 합친`plaza_repair_bounded_candidate.png`는 마스크 밖 변경0픽셀을 검사했다. `first_pair_repaired_candidate.png`는 이 후보의 연결 미리보기, `plaza_bounded_comparison.png`는 비교표다. 다만 잔디·포장·식재가 다각형 경계에서 끊겨 배포용으로 채택하지 않았다. 비용/검수 기록은`repair_review.json`을 기준으로 한다.
- 다음 보정 전에 직선 다각형 대신 실제 보도·녹지 경계에 맞춰 영역을 정하고 건물/도로 보호 마스크를 검토해야 한다. 마스크 밖 보존과 마스크 안 지형 정확성은 별도 검사다. 첫2장의 출력 좌표·폭·도로4px 검증과 나머지22장 생성은 여전히 미완료다.
- 이하2회/$0.168644·승인 대기·503 기록은 **이전 단계 이력**이다. 현재 보정1회 승인은 소비 완료됐고, 추가 유료 보정은 실행하지 않는다.
- 최신 결과(2026-09-17): 제공자 조회가 복구된 후 사용자 재개 지시로 첫2장을 생성했다. `background_2_2`는$0.082703/52.487초, `background_2_3`는$0.085941/48.177초. **누적2회/$0.168644**. 기존$5/32회 한도는 유지한다.
- 결과 폴더는`prototype2/work/dense-city-20260916-source/`. `first_pair_preview.png`는768px 코어2개를 그대로 붙인1536×768 시안이다. `generation_tasks.json`에 실제 프롬프트, 각`*_intent.json`에 입력SHA, `report.json`에 실제 과금 기록이 있다. 기존 OpenRouter API를 사용했다.
- 지붕 비교·도로 경계 확대를 직접 확인했다. 큰 도로 흐름과 주요 건물은 유지됐으나, 원본에 없는 조경과 겹침 영역의 색감·세부 변경이 남았다. 수치 기반 출력 좌표/폭/4px 경계 검증까지 통과한 것으로 간주하지 않았다. `firstPairPassed=false`, 나머지22장 미생성.
- 중앙 광장만 고치는`plaza_repair_1`을 준비했으나 실행 전 자동 승인 검토가 거절했다. 기존 생성물·원본·영역 마스크를 외부 OpenRouter→OpenAI에 재전송하는 추가 유료 보정이 첫2장 승인 범위를 넘는다는 사유다. 보정 요청은 전송하지 않았고 추가 비용0이다. 우회하지 않았으며 별도 사용자 승인이 필요하다. 현재 상태는`pair_status.json`에 기록했다.
- 아래0회/$0 및HTTP503 기록은 **첫 생성 이전의 이력**이다. 최신 누계는 위2회/$0.168644를 기준으로 한다.
- 2026-09-17 사용자가 명시한 외부 전송 범위와$5 한도를 승인하여 재개했다. 첫 생성 전 최신 모델·가격 조회(`GET /images/models/openai/gpt-image-2.5-sunburst/endpoints`)가 HTTP503으로 실패했다. 이전 가격으로 우회하거나 자동 재시도하지 않고 중단했다. 유료 POST·예약 intent 생성 전 실패하여 누적0회/$0이다.
- 재개 기록: `prototype2/work/dense-city-20260916-source/resume-20260917.json`. 외부 전송 승인 자체는 해결됐으며, 현재 대기는 제공자 메타데이터 조회 실패다. 다음 재개 시 최신 가격·기능 확인부터 수행한다.
- 24개1024² 원본과4608×3072 합성 원본을 확보했다. 경복궁·광화문·종로타워의3D 자료를 시각 확인했다.
- 원본100m=256px, 공통점의 수학적 정렬 오차 최대`4.25e-9px`. 렌더링 LOD 차이 때문에 실제 겹침의 RGB 평균 차이는`0.024~5.133/255`이며, 생성 결과 연결 통과를 뜻하지 않는다.
- 첫 인접 후보는`background_2_2`와`background_2_3`. 각각 구조2점과 지붕 너비102.59px/145.77px를 생성 전에 고정했다.
- 2026-09-16 첫 유료 요청이 실행 전 자동 승인 검토에서 차단됐다. 로컬 지도·스타일 참조 이미지를 외부 OpenRouter로 전송하는 범위의 명시적 승인이 필요하다는 사유다. 우회하거나 재시도하지 않았다.
- **이번 이미지 요청0회, 비용$0.** 요청 intent·AI 결과 없음. 가이드 실호출도0회.
- 예정 전송처: OpenRouter API → OpenAI 제공자, `openai/gpt-image-2.5-sunburst`. 첫 요청 자료: `source_2_2.png`, 사용자 게임 스타일 참조`style.png`, 광화문 제거 마스크`gwang_remove_2_2.png`, 기존 지도 색상 참조`palette.png`, 고정 좌표가 포함된 프롬프트. API 키는 인증에만 사용하며 이미지·프롬프트에 포함하지 않는다.
- 전체 이미지 예산$5/최대32회는 유지하며, 첫2장 검수 전 후속22장 생성 금지도 유지한다. 승인 대기는 자료의 이용권이 확인됐다는 뜻이 아니다.
- 원본·프롬프트·검증점·스크립트 보관: `prototype2/work/dense-city-20260916-source/`(Git/공개 배포 제외). `source_full_preview.png`는 **AI 완성본이 아닌 원본 수집 미리보기**다.
- 첫2장 품질, 전체 경계, 독립 객체3곳, 실제 도로 차량 구간, 실제 결과 브라우저 검수·배포가 남아 있다. 이 관문을 통과한`delivery.json`은 아직 만들지 않았다.

## 재현

```sh
node --test prototype2/tests/test_dense_map.mjs
prototype2/.venv/bin/python -m unittest discover -s prototype2/tests -p 'test_dense_snapshot.py'
prototype2/.venv/bin/python prototype2/tests/dense_browser_check.py --dest prototype2/work/dense-renderer-qa-NEW
# 실제 검수 완료 스냅샷이 준비된 뒤에만:
prototype2/.venv/bin/python prototype2/tests/dense_browser_check.py --snapshot prototype2/assets/city_dense --dest prototype2/work/dense-real-qa-NEW
prototype2/.venv/bin/python prototype2/scripts/dense_snapshot.py --source <검수완료자료> --dest prototype2/assets/city_dense
prototype2/.venv/bin/python prototype2/scripts/city_release.py --snapshot prototype2/assets/city_dense --dest prototype2/work/city-release-NEW --deployment-decision <지시기록JSON>
```

로컬 고밀도 진입점은 개발 서버의 `/?dense=1`이며, 실제 스냅샷이 준비되기 전에는 사용하지 않는다.
기본 `/`와 기존 공개 사이트는 기존 지도다. 원격 푸시·main 병합은 이번 범위에 없다.
